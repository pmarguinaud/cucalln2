MODULE YOMPARALLELMETHOD

USE PARKIND1, ONLY : JPIM

IMPLICIT NONE

CONTAINS

LOGICAL FUNCTION LPARALLELMETHOD (CDMETHOD, CDNAME) 

USE XRD_UNIX_ENV

IMPLICIT NONE

CHARACTER (LEN=*),   INTENT (IN) :: CDMETHOD
CHARACTER (LEN=*),   INTENT (IN) :: CDNAME


CHARACTER (LEN=*), PARAMETER :: CLFILE = 'lparallelmethod.txt'
CHARACTER (LEN=256) :: CLMETHOD1, CLMETHOD2

CHARACTER (LEN=256), ALLOCATABLE, SAVE :: CLNAMES (:)
CHARACTER (LEN=256), ALLOCATABLE, SAVE :: CLMETHODS (:)
LOGICAL, SAVE :: LLVERBOSE = .FALSE.
LOGICAL, SAVE :: LLINIT = .FALSE.

INTEGER :: ICOUNT, IERR, I, IDX
CHARACTER (LEN=16) :: CLENV

IF (.NOT. LLINIT) THEN

  CALL XRD_COUNTLINES (ICOUNT, CLFILE, IERR)
  CALL GETENV ('LPARALLELMETHOD_VERBOSE', CLENV)
  LLVERBOSE = TRIM (CLENV) /= '' .AND. TRIM (CLENV) /= '0'

  IF (IERR == 0) THEN

    ALLOCATE (CLNAMES (ICOUNT), CLMETHODS (ICOUNT))
    OPEN (77, FILE=CLFILE, FORM='FORMATTED')
    DO I = 1, ICOUNT
      READ (77, *) CLMETHODS (I), CLNAMES (I)
    ENDDO
    CLOSE (77)

!   IF (LLVERBOSE) THEN
      WRITE (0, *) "------ "//TRIM (CLFILE)//" ------"
      DO I = 1, SIZE (CLNAMES)
        WRITE (0, '(A32," ",A)') CLMETHODS (I), TRIM (CLNAMES (I))
      ENDDO
      WRITE (0, *) "------ "//TRIM (CLFILE)//" ------"
!   ENDIF

  ENDIF

  LLINIT = .TRUE.

ENDIF

CALL XRD_UPPER_CASE (CLMETHOD1, CDMETHOD)

IDX = -1

IF (ALLOCATED (CLNAMES)) THEN
  DO I = 1, SIZE (CLNAMES)
    IF ((TRIM (CDNAME) == TRIM (CLNAMES (I))) .AND. (CLMETHODS (I)(1:1) /= '#')) THEN
      CALL XRD_UPPER_CASE (CLMETHOD2, CLMETHODS (I))
      LPARALLELMETHOD = TRIM (CLMETHOD1) == TRIM (CLMETHOD2)
      IDX = I
      GOTO 999
    ENDIF
  ENDDO
ENDIF

LPARALLELMETHOD = (CLMETHOD1 == "OPENMP") .OR. (CLMETHOD1 == "OPENMPMETHOD")

999 CONTINUE

IF (LLVERBOSE .AND. LPARALLELMETHOD) THEN
  IF (IDX > 0) THEN
    WRITE (0, '(A32," ",A)') CLMETHODS (I), TRIM (CLNAMES (I))
  ELSE
    WRITE (0, '(A32," ",A)') CLMETHOD1, TRIM (CDNAME)
    CALL FLUSH(0)
  ENDIF
ENDIF

END FUNCTION

LOGICAL FUNCTION LSYNCHOST (CDNAME) 

USE XRD_UNIX_ENV

IMPLICIT NONE

CHARACTER (LEN=*),   INTENT (IN) :: CDNAME


CHARACTER (LEN=*), PARAMETER :: CLFILE = 'lsynchost.txt'

CHARACTER (LEN=256), ALLOCATABLE, SAVE :: CLNAMES (:) 

LOGICAL, SAVE :: LLVERBOSE = .FALSE.
LOGICAL, SAVE :: LLINIT = .FALSE.

INTEGER :: ICOUNT, IERR, I
CHARACTER (LEN=16) :: CLENV

LSYNCHOST = .FALSE.

IF (.NOT. LLINIT) THEN

  CALL XRD_COUNTLINES (ICOUNT, CLFILE, IERR)
  CALL GETENV ('LSYNCHOST_VERBOSE', CLENV)
  LLVERBOSE = TRIM (CLENV) /= '' .AND. TRIM (CLENV) /= '0' 

  IF (IERR == 0) THEN

    ALLOCATE (CLNAMES (ICOUNT))
    OPEN (77, FILE=CLFILE, FORM='FORMATTED')
    DO I = 1, ICOUNT
      READ (77, *) CLNAMES (I) 
    ENDDO
    CLOSE (77)

!   IF (LLVERBOSE) THEN
      WRITE (0, *) "------ "//TRIM (CLFILE)//" ------"
      DO I = 1, SIZE (CLNAMES)
        WRITE (0, '(A)') TRIM (CLNAMES (I))
      ENDDO
      WRITE (0, *) "------ "//TRIM (CLFILE)//" ------"
!   ENDIF

  ENDIF

  LLINIT = .TRUE.

ENDIF

IF (ALLOCATED (CLNAMES)) THEN
  DO I = 1, SIZE (CLNAMES)
    IF ((TRIM (CDNAME) == TRIM (CLNAMES (I))) .AND. (CLNAMES (I)(1:1) /= '#')) THEN
      LSYNCHOST = .TRUE.
      WRITE (0, '(A32," ",A)') "SYNCHOST", TRIM (CLNAMES (I))
      CALL FLUSH(0)
      RETURN
    ENDIF
  ENDDO
ENDIF

END FUNCTION


END
